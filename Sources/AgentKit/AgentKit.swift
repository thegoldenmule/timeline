// AgentKit: the tool registry (`EditorToolRegistry`, `EditorTools`), the hand-maintained JSON Schemas
// (`OperationSchemas`, `JSONSchemaValidator`), the MCP server (`MCPServerHost`), the Claude Code CLI
// sidecar (`ClaudeCodeRuntime`, `StreamJSONParser`), the Messages-API stub (`MessagesAPIRuntime`), the
// stdio proxy library (`StdioProxy`), and the bundled Skills (`SkillsInstaller`).
// See docs/design/implementation-plan.md (AgentKit row) and docs/design/contracts-notes.md.
